## How to run

1) have a Node runtime installed (tested on v22.16.0)
2) install `tsx` with `npm install -g tsx`
3) inside the folder, run `npm install`
4) inside the folder, run with `npx tsx main.ts`
